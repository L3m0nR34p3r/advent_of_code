data = open('Day 6/input.txt','r').read()

# Need to find a way to advance this by one letter every time it runs. And add a way to check if there's a duplicate letter in a chunk.
def fours(data): 
    chunked_list = list()
    split = 4
    for i in range(0, len(data),split):
        chunked_list.append(data[i:i+split])
    print(chunked_list)

fours(data)

# four = [(x.split()) for x in four]

# # for x in range([0],[3]):
# print(four)

# # for index, value in enumerate(four):
# #     if index % 4 == 0:
# #         # print(index, value)
